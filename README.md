# External-Overlay-ImGui

The point of the source code is to place your menu ontop of your target window and clip it onto it.
Sleep(100); -> this is for performance but might cause a minor delay when minimising the target window change to 0 to remove the delay
